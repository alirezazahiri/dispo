import React from "react";
import { RequestTab } from "../types";

type RequestEditorProps = {
  tab: RequestTab;
};

export const RequestEditor: React.FC<RequestEditorProps> = ({ tab }) => {
  return null;
};
