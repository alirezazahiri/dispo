import React from "react";
import { RequestTab } from "../../types";

type ResponsePanelProps = {
  tab: RequestTab;
};

export const ResponsePanel: React.FC<ResponsePanelProps> = ({ tab }) => {
  return null;
};
