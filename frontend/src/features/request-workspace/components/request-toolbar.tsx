import React from "react";
import { RequestTab } from "../types";

type RequestToolbarProps = {
  tab: RequestTab;
};

export const RequestToolbar: React.FC<RequestToolbarProps> = ({ tab }) => {
  return null;
};
